## Packages
framer-motion | Smooth page transitions and element animations
react-day-picker | Date picker component for booking forms
date-fns | Date formatting and manipulation
react-hook-form | Form state management
@hookform/resolvers | Zod integration for forms
clsx | Conditional class names
tailwind-merge | Merging tailwind classes

## Notes
Authentication uses the `use-auth.ts` hook provided by Replit Auth.
API endpoints are defined in `shared/routes.ts`.
Images use Unsplash placeholders where appropriate.
Theme colors: Primary #1A3C5E, Secondary #F4A020, Accent #00C9A7.
